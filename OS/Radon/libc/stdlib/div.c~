#include <stdlib.h>

typedef struct {
  int quot;
  int rem;
} div_t;

div_t div(int numer, int denom)
{
    div_t result;
    int q = 0;
    int r = 0;
    while(denom*q < numer)
    {
        q++;
    }
    result.quot = --q;
    result.rem = numer - denom*q;
    return result;
}
